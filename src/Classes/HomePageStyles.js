import { makeStyles } from "@mui/styles";
import { color } from "@mui/system";
const HomePageStyles = makeStyles((theme) => ({
  homePageContainer: {
    padding: "1rem",
    width: "60%",
    height: "100%",
    backgroundColor: "white",
    marginTop: "4rem !important",
    color: "black",
    margin: "0rem 7rem 3rem auto",
  },

}));

export default HomePageStyles;
